// Fixture header for the kai_workspace example. Real KAI headers live
// in Christian's actual KaiWorkspace repo; this file exists only so
// examples/kai_workspace.lm's include(kai_core, ...) path resolves to
// something real, and so the generated CMakeLists.txt can be
// configured (not built — these are stubs) as an accuracy check.
#pragma once

namespace kai_core {
void initialize();
}
