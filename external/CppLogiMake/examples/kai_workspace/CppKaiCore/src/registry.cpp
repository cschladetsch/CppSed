// Second source file, deliberately present so the git-ls-files-backed
// source resolution in resolver.cpp has more than one file to prove
// it actually enumerates a directory rather than hardcoding one path.
namespace kai_core::registry {
int count = 0;
}
